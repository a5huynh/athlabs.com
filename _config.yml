safe: false
incremental: true
lsi: false

exclude: [
  '.*',
  '_config.yml',
  '*.sublime*',
  'Gemfile',
  'Gemfile.lock',
  's3_website.yml',
  'README.md'
]

sass:
  style: :compressed

defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
